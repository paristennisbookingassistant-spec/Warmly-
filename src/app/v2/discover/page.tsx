/**
 * app/v2/discover/page.tsx — scaffold placeholder (replaced in P1).
 */

import Placeholder from "../Placeholder";

export default function V2DiscoverPage() {
  return (
    <Placeholder
      eyebrow="Discover"
      title="Two channels to find your next warm intro."
      body="The two-door chooser and the swipe deck land in Phase 1. The LinkedIn door will run on your real synced connections; the INSEAD directory follows."
    />
  );
}
